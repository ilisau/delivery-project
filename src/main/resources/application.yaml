spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/delivery_project
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: never
      data-locations: classpath:data.sql
      continue-on-error: false